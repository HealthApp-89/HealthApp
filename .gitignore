# dependencies
node_modules
.pnp
.pnp.js

# next.js
.next
out
next-env.d.ts

# production
build
dist

# misc
.DS_Store
*.pem
~$*

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (NEVER commit)
.env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# IDE
.idea
.vscode

# prototype (kept locally for reference, never committed)
_prototype.jsx
.claude/

# worktrees (isolated workspaces — never tracked)
.worktrees/

# supabase CLI tooling state
supabase/.temp/

# superpowers brainstorm session files
.superpowers/

# UX walkthrough deck — generated build artifacts (source under scripts/presentation/)
docs/Apex-Health-OS-*.docx
docs/Apex-Health-OS-*.pptx
scripts/presentation/shots/
scripts/presentation/shots-real/

# one-off dev fixup scripts (committed scripts/ utilities follow the audit-*/import-* pattern)
scripts/insert-measurement-*.mjs
scripts/reset-today-intake.mjs

# Local-only WHOOP data archive from the Garmin cutover (never publish health data)
docs/superpowers/whoop-daily-archive-*.json
